export const metadata = {
  title: { absolute: "ASTGSE | Cookie policy" },
  robots: { index: false },
};

export default function CookiePolicyLayout({ children }: { children: React.ReactNode }) {
  return children;
}
